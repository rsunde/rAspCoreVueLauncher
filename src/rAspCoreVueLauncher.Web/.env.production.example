# API base URL for production builds.
# Leave unset for same-origin (web behind a reverse proxy).
# Tauri desktop: point to the sidecar (http://127.0.0.1:<port>)
# Capacitor mobile: point to your LAN IP or production API host
# VITE_API_BASE_URL=https://api.example.com
